apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "join-github.fullname" . }}-test-connection"
  labels:
    {{- include "join-github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "join-github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
